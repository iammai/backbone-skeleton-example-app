<div class="">
    <div class="card-panel">
        <a class="btn-floating btn-large waves-effect waves-light pink upvote"><i class="mdi-maps-navigation"></i><span class="upvote-count">{{model.upvotes}}</span></a>
        <span class="story-header">{{model.name}}</span>
    </div>
</div>
